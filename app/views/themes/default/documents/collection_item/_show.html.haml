%article.collectionItem.draggableItem.span-14{:class => document.published? ? 'published' : 'notPublished', :id => "document_#{document.id}"}
  .content
    :textile
      #{textile_wiki_filter(document.content)}

  = render :partial => 'actions', :locals => {:document => document, :draggable => document.published?}
